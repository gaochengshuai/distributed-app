server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: g478523241
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    out-of-order: false
# RabbitMQ 配置 - 如果不需要消息队列功能，可以注释掉以下配置
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: admin
#    password: 123456
